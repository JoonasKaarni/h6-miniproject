- name: Install Docker
  apt:
    name:
      - docker.io
      - docker-compose
    state: present

- name: Start Docker
  service:
    name: docker
    state: started
    enabled: yes

- name: Add user
  user:
    name: "user"
    groups: docker
    append: yes
